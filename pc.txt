git init

ls -a (shows hidden files)

git add .
git status

git commit -m "initial commit"  (writing a message "initial commit")

git remote add origin https://github.com:spsh1/Portfolio.git   (to make changes to the github portfolio)

git config --list
git branch
git branch -M main


git push -u origin main

git config user.email "sprihasharma098@gmail.com" (only if after config --list garepaxi email show garena bhane)



